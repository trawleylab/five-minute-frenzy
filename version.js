/* Single source of truth for this app's version.
   The home screen shows it and the service worker names its cache after it,
   so you can always tell at a glance what an installed iPad is running.
   Bump with ../bump.sh five-minute-frenzy — never edit the number in two places. */
self.APP_VERSION = "1";
self.APP_DATE = "2026-09-05";
